import { BoxLayout, Button, Spacer, TabNav } from 'opize-design-system';
import { client } from '../../../lib/client';

export function AdminToolsContainer() {
    return (
        <>
            <Spacer height="20px" />
            <BoxLayout minHeight="calc(100vh - 131px - 337px)"></BoxLayout>
        </>
    );
}
