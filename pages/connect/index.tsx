import type { NextPage } from 'next';
import { SlideBoxContainer } from '../../containers/connect/connect.container';

const Home: NextPage = () => {
    return <SlideBoxContainer />;
};

export default Home;
